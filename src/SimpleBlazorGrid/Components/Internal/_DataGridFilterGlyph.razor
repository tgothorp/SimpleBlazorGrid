@* ReSharper disable once CheckNamespace *@
@namespace SimpleBlazorGrid.Internal

<svg class="simpleDG_glyph @(Active ? "simpleDG_glyph_active" : "")" viewBox="0 0 20 20" style="position: absolute; height: 100%; max-height: 20px">
    <polygon points="2,0 2,3 8,10 8,20 12,16 12,10 18,3 18,0"/>
</svg>

@code {
    [Parameter]
    public bool Active { get; set; }
}