@page "/Customers"

@using SimpleBlazorGrid.EntityFramework.DataSource
@using SimpleBlazorGrid.EntityFramework.Sandbox.Core.Domain
@using SimpleBlazorGrid.EntityFramework.Sandbox.Core.Infrastructure

<SimpleGrid TType="Customer"
            Title="Customers"
            Selectable="@true"
            SimpleGridDataSource="@(new SimpleGridEntityFrameworkDataSource<Customer>(Context.Customers.AsQueryable()))">
    <Filters>
        @* <SimpleFilter TType="Customer" Property="@nameof(Customer.CustomerId)"/> *@
    </Filters>
    <Columns>
        <SimpleColumn TType="Customer" For="@(x => x.CustomerId)" Sortable="true" Heading="Id"/>
    </Columns>
</SimpleGrid>

@code {

    public DatabaseContext Context { get; set; } = new DatabaseContext();
}